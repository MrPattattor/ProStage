{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block contenu %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>NomLong</th>
                <td>{{ formation.nomLong }}</td>
            </tr>
            <tr>
                <th>NomCourt</th>
                <td>{{ formation.nomCourt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pro_stage_formations') }}">Retour à la liste des formations</a> </br>

    <a href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier cette formation</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
