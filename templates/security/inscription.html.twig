{% extends 'base.html.twig' %}

{% block contenu %}
<center>
<h3 class = "display-4">
    Enregistrez-vous
</h3>
</br></br>
</center>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-9">
                {{ form_start(vueFormulaire, {'attr':{'novalidate':'novalidate'}}) }}

                <label>Prénom</label>
                {{ form_widget(vueFormulaire.prenom, {'attr' : {'placeholder':"Votre prenom..."}}) }}
                {{ form_errors(vueFormulaire.prenom) }}<br>

                <label>Nom</label>
                {{ form_widget(vueFormulaire.nom, {'attr' : {'placeholder':"Votre nom..."}}) }}
                {{ form_errors(vueFormulaire.nom) }}<br>

                <label>Email</label>
                {{ form_widget(vueFormulaire.email, {'attr' : {'placeholder':"Votre email..."}}) }}
                {{ form_errors(vueFormulaire.email) }}<br>

                <label>Mot de passe</label>
                {{ form_widget(vueFormulaire.password, {'attr' : {'placeholder':"Votre mot de passe..."}}) }}
                {{ form_errors(vueFormulaire.password) }}<br>

                <button type="submit" class="btn btn-dark">S'enregister</button>

                {{ form_end(vueFormulaire) }}
            </div>
        </div>
    </div>
{% endblock %}